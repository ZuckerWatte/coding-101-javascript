export function isThisNumberEven(number: number): boolean {
  //   return number % 2 === 0 ? true : false;
  return null;
}

export function isThisNumberPositive(number: number): boolean {
  // return number >= 0 ? true : false;
  return null;
}
